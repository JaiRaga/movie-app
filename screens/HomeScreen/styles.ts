import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	container: {
		flex: 1,
		padding: 10,
		marginTop: 10,
		// backgroundColor: '#fff',
	},
	heading: {
		fontSize: 20,
		fontWeight: 'bold',
	},
	image: {
		width: 110,
		height: 200,
		resizeMode: 'cover',
		borderRadius: 5,
		margin: 5,
	},
})

export default styles
